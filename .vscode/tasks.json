{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
      {
        "type": "shell",
        "label": "g++ build active file",
        //"command": "make",
        "command": "g++ -g -std=c++11 kdtree_1d.cc",
        "args": [],
        "options": {
          "cwd": "${workspaceFolder}"
        },
        "group": "build"
      }
    ]
  }